# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TriviaFlop' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TriviaFlop
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'Firebase/Database'
  pod 'HTMLString', '~> 4.0'
  pod 'SwiftySound'

end
